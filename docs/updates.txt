ver 3.3.2

JP:
- バグ修正 : 広告表示時にクラッシュすることがあったのを修正
- バグ修正 : iPad で PIN 入力画面が表示されない問題を修正

EN:
- Bug fix: crashes with google ads.
- Bug fix: PIN view is not showed on iPad.

---
ver 3.3

JP:
以下の機能改善を行いました
- iOS4: バックグランドから復帰するときにPINを確認するようにした
- 摘要入力画面のUIを改良
- 取引を削除しても摘要履歴が残るようにした
- 資産一覧画面で、資産合計値をツールバーに表示
- 日本語CSVファイルをShift-JISエンコーディングで出力するようにした

EN:
- iOS4: Check PIN code when app resumes from background state.
- Modify UI of description input screen.
- Keep description histories even if transactions are deleted.
- Show total balance on toolbar of assets screen.
- Export Shift-JIS encoded CSV file for Japanese locale.


---
ver 5.5.2

JP:
iPad版で回転時にクラッシュすることがある問題を修正

EN:
Bug fix: Crash may occur when rotate screen with iPad.

---
ver 6.0.0

JP:
注意: ver 6.0.0 からバックアップデータのフォーマットが変更になりました。
旧バージョンのバックアップデータはそのまま読み込めますが、新バージョンのバックアップデータは旧バージョン(iOS/Androidともver 6.0.0 未満のバージョン)では読み込めませんのでご注意ください。

修正点:
- データベースおよびバックアップデータフォーマットを更新
- 翻訳を改善

EN:
CAUTION: The backup data format was changed from ver 6.0.0.
An older version data can be read with this version app, but new version data can't be read with old version app (before ver 6.0.0, both iOS/Android).

Changes:
- Database and backup data format was updated.
- Improve translations.
